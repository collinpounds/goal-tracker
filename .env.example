# Environment: production or local
ENVIRONMENT=local

# =============================================================================
# SUPABASE CONFIGURATION
# Get these from your Supabase project dashboard: Settings -> API
# =============================================================================

# Project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Public anon key (safe for client-side, protected by RLS)
SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key - KEEP SECRET! (bypasses RLS, server-side only)
# Get from: Settings -> API -> Project API keys -> service_role
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# JWT VERIFICATION (choose one method)
# =============================================================================

# RECOMMENDED: RS256 with JWKS (asymmetric, more secure)
# Get from: Settings -> API -> JWT Settings -> JWKS URL
# Format: https://your-project-ref.supabase.co/auth/v1/.well-known/jwks.json
SUPABASE_JWKS_URL=https://your-project-ref.supabase.co/auth/v1/.well-known/jwks.json

# LEGACY (deprecated): HS256 with shared secret
# Only use if JWKS is not available (e.g., older Supabase projects)
# SUPABASE_JWT_SECRET=your-jwt-secret-here

# =============================================================================
# LOCAL DEVELOPMENT (Supabase CLI)
# =============================================================================
# Run: supabase start
# Then use these values:
# SUPABASE_URL=http://localhost:54321
# SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
# SUPABASE_JWT_SECRET=super-secret-jwt-token-with-at-least-32-characters-long
# Note: Local Supabase uses HS256, not JWKS

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================
PORT=8000

# =============================================================================
# FRONTEND CONFIGURATION
# These start with VITE_ to be available in the frontend build
# =============================================================================
VITE_API_URL=http://localhost:8000
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
